---
import JournalEntry from "./JournalEntry.astro";
const allEntries = await Astro.glob('../pages/journal/*.mdx');
const entries = allEntries.filter((entry) => !entry.frontmatter?.draft);
---

<div class="flex pt-32 md:justify-center max-md:pt-20 max-md:pl-1">
  <h3
    data-mobile-tippy="true"
    data-tippy-content="No rhyme or reason to the writing here. My hope is that having a place to publish will be an incentive to think about things more deeply. (We'll see. TBD)"
    class="font-headline text-9xl text-purple text-vertical whitespace-nowrap z-10 pr-8"
  >
    Journal
  </h3>
</div>

<div class="py-32 max-md:py-20 max-md:pl-6">
  {entries.map((entry, index) => {
    const orientation = index % 2 === 0 ? 'right' : 'left';

    return <JournalEntry title={entry.frontmatter.title} url={entry.url} tags={entry.frontmatter.tags} orientation={orientation} />;
  })}
</div>
