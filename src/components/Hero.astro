<div class="relative h-[100dvh]">
  <div class="h-[100dvh] text-center relative flex justify-center items-center overflow-x-hidden">
    <div class="-translate-y-[80px] max-sm:-translate-y-[100px]">
      <h2 id="hero" class="hero font-headline text-hero text-indigo font-display opacity-10 sm:whitespace-nowrap">
        <span class="ir-wide-rounds">P</span>roduct<br class="sm:hidden" /> & Design
      </h2>

      <div class="absolute inset-0 flex items-center justify-center -mt-16">
        <h1 class="text-[12.1vw] text-amber font-mono italic leading-none -rotate-15">
          <div class="flex flex-col items-center justify-center">
            <div class="flex">
              <span class="hvr-bounce-in">M</span>
              <span class="hvr-bounce-in">a</span>
              <span class="hvr-bounce-in">t</span>
              <span class="hvr-bounce-in">t</span>
            </div>
            <div class="flex">
              <span class="hvr-bounce-in">S</span>
              <span class="hvr-bounce-in">t</span>
              <span class="hvr-bounce-in">e</span>
              <span class="hvr-bounce-in">n</span>
              <span class="hvr-bounce-in">b</span>
              <span class="hvr-bounce-in">a</span>
              <span class="hvr-bounce-in">c</span>
              <span class="hvr-bounce-in">k</span>
            </div>
          </div>
        </h1>
      </div>
    </div>
  </div>

  <img
    src="/images/me.png"
    alt="Matt Stenback"
    class="absolute bottom-0 left-1/2 -translate-x-1/2 h-[55vh] max-sm:h-[44vh] object-contain object-bottom pointer-events-none mix-blend-multiply"
  />

  <p class="max-sm:text-xs absolute top-8 left-6 md:left-10 font-mono italic text-sm">
    Matt Stenback
  </p>

  <p class="max-sm:text-xs absolute top-8 right-6 md:right-10 font-mono italic text-sm text-right">
    <span class="max-sm:hidden">Currently </span><span class="sm:hidden">A</span><span class="max-sm:hidden">a</span>t <a href="https://spellbook.legal" target="_blank">Spellbook</a>
  </p>

  <p class="max-sm:text-xs absolute bottom-8 left-6 md:left-10 font-mono italic text-sm">
    <a href="mailto:hello@mattstenback.com">
      Email
    </a>
  </p>

  <p class="max-sm:text-xs absolute bottom-8 right-6 md:right-10 font-mono italic text-sm">
    <a href="https://www.instagram.com/stenback" target="_blank">
      Instagram
    </a>
  </p>
</div>

<script>
  import { hasTouchScreen } from '../scripts/touch-screen.js';

  if (!hasTouchScreen) {
    const root = document.documentElement;
    let throttle = false;
    const throttleInterval = 20; 

    // Calculate the offset of the mouse position relative to the center of the screen and apply it to CSS variables to animate the hero text
    root.addEventListener("mousemove", e => {
      if (throttle) return;

      throttle = true;
      setTimeout(() => throttle = false, throttleInterval);

      const w = window.innerWidth;
      const h = window.innerHeight; 

      // Determine the xy orientation relative to the center of the screen
      let xSign = e.clientX > w/2 ? 1 : -1;
      let ySign = e.clientY > h/2 ? 1 : -1;

      // Calculate the distance from the center of the screen as an integer
      let xDist = Math.round(Math.abs(w/2 - e.clientX));
      let yDist = Math.round(Math.abs(h/2 - e.clientY));

      root.style.setProperty('--offsetX', (xDist * xSign).toString());
      root.style.setProperty('--offsetY', (yDist * ySign).toString());
    });
  }
</script>

